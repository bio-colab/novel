# ==============================================================================
# WORLD STATE SNAPSHOT: {{WORLD_TITLE}}
# Snapshot ID: STATE-INITIAL
# ==============================================================================

metadata:
  schema_version: "1.0.0"
  world_id: "{{WORLD_ID}}"
  snapshot_id: "STATE-INITIAL"
  timeline_minute: 0

environment:
  ambient_temperature_c: 20.0
  ambient_humidity_percent: 50.0
  illumination_lux: 25.0

zone_alpha:
  heating_required: true
  power_status: "nominal"

world:
  water_rationing_active: true
  quarantine_active: true

timeline:
  elapsed_time_hours: 0.0
  confinement_hours: 0.0
